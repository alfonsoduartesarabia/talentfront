server:
  port: 8080
  servlet:
    context-path: /backend

app:
  host-name: ${HOSTNAME:localhost}
spring:
  datasource:
    url: jdbc:mysql://${DB_LOCATION:localhost}:3306/db?verifyServerCertificate=false&useSSL=true
    username: freya
    password: belphagor
    driverclassnamee: com.mysql.jdbc.Driver
